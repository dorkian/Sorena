# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Test/lint caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Secrets/env
.env
.env.*

# Local model artifacts (whisper.cpp / Piper / Ollama / embeddings — later phases)
*.gguf
*.onnx
*.ggml
models/
traces/

# Exception: the small (~200KB) custom-trained wake word model is committed —
# unlike downloaded voice/STT weights, it can't be re-fetched, only retrained
!models/wakeword/
!models/wakeword/*.onnx

# Local voice-pipeline test recordings (Phase 4 diagnostics)
*.wav

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local code-graph tool output (generated)
.codegraph/

# Long-term memory DB (Phase 5) — local conversation history, not shareable
data/
